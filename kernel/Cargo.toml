[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
bitflags = "2.9.0"
bootloader_api = "0.11.10"
lazy_static = { version = "1.5.0", features = ["spin_no_std"] }
